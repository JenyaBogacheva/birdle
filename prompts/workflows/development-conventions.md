# Prompt 3. Generate Development Conventions — @conventions.md

Create a file called conventions.md with the coding rules for this project.

These rules will be passed to the code assistant that generates code.

The conventions must:

- reflect all key development principles from @vision.md for the bird-ID MVP
- reference @vision.md instead of duplicating details
- be concise and follow KISS
- include only what directly affects code quality and maintainability
- avoid unnecessary complexity

After generating conventions.md:

- add this prompt to the current prompt session so it becomes part of the project's prompt workflow and can be reused later.

